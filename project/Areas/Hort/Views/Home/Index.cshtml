@{
    ViewData["Title"] = "Панель Адміністратора";
}
<div style="margin-left:50px">
<h2>@ViewData["Title"]</h2>
<div id="main-wrapper">
        <div class="inner">
                <div>
                    <h3>Статті</h3>
                    <div>
                        <a asp-area="Hort" asp-controller="ServiceItems" asp-action="Edit" asp-route-id="">Створити стату</a>
                    </div>
                    <div>
                        @foreach(var title in ViewBag.Name)
                        {
                            <h4>@title</h4>
                        <div>
                            @foreach (var entity in ViewBag.ServiceItems as IQueryable<ServiceItem>)
                            {
                                @if (entity.Father == title)
                                {
                                    <div>
                                        <a asp-area="Hort" asp-controller="ServiceItems" asp-action="Edit" asp-route-id="@entity.Id">редагувати</a>
                                        |
                                        <form style="display: inline-block;" id="form-@entity.Id" asp-area="Hort" asp-controller="ServiceItems" asp-action="Delete" method="post">
                                            <input type="hidden" name="id" value="@entity.Id">
                                            <a href="#" onclick="document.getElementById('form-@entity.Id').submit();">видалити</a>
                                        </form>
                                        |
                                        <a asp-area="" asp-controller="Services" asp-action="Index" asp-route-id="@entity.Id">
                                            @($"{entity.Title}")
                                        </a>
                                    </div>
                                }
                                
                            }
                        </div>
                        }
                    </div>
                </div>
                <div>
                    <h3>Сторінки сайту</h3>
                    <div>
                        <a asp-area="Hort" asp-controller="TextFields" asp-action="Edit" asp-route-id="">Створити сторінку</a>
                    </div>
                    <div>
                        <h3>Редагувати сторінки сайту</h3>
                        <div>
                            @foreach (var entity in ViewBag.TextFields as IQueryable<TextField>)
                            {
                                <div>
                                    <a asp-area="Hort" asp-controller="TextFields" asp-action="Edit" asp-route-codeWord="@entity.CodeWord">редагувати</a>
                                    |
                                    <form style="display: inline-block;" id="form-@entity.Id" asp-area="Hort" asp-controller="TextFields" asp-action="Delete" method="post">
                                        <input type="hidden" name="id" value="@entity.Id">
                                        <a href="#" onclick="document.getElementById('form-@entity.Id').submit();">видалити</a>
                                    </form>
                                    |
                                    <a asp-area="" asp-controller="Home" asp-action="@entity.View" asp-route-codeWord="@entity.CodeWord">
                                        @($"{entity.Title}")
                                    </a>
                                </div>
                            }
                        </div>
                    </div>
                </div>
                <div>
                    <h3>Фотографії</h3>
                    <div>
                        <a asp-area="Hort" asp-controller="PhotoFields" asp-action="Edit" asp-route-id="">Додати фотографію</a>
                    </div>
                    <div>
                        <h3>Додавання фотографії</h3>
                        <div>
                        @foreach (var entity in ViewBag.PhotoFields as IQueryable<PhotoField>)
                            {
                                <div>
                                    <form style="display: inline-block;" id="form-@entity.Id" asp-area="Hort" asp-controller="PhotoFields" asp-action="Delete" method="post">
                                        <input type="hidden" name="id" value="@entity.Id">
                                        <a href="#" onclick="document.getElementById('form-@entity.Id').submit();">видалити</a>
                                    </form>
                                    |
                                    <a href="#">
                                        @entity.Title
                                    </a>
                                </div>
                            }
                        </div>
                    </div>
                </div>
                <div>
                    <form asp-area="" asp-controller="Account" asp-action="Logout" method="post">
                        <input type="submit" value="Вийти" />
                    </form>
                </div>
        </div>
    </div>
</div>
